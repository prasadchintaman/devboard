name: DevSecOps

on:
    push:
        branches: [advanced]

jobs:

    code-quality:
        uses: ./.github/workflows/code-quality.yml

    secret-scanning:
        uses: ./.github/workflows/secrete-scanning.yml